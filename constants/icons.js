import eyeOpen from "../assets/icons/eye-open.png"; // Ensure the path and format are correct
import eyeClosed from "../assets/icons/eye-closed.png"; // Ensure the path and format are correct

export default {
    eyeOpen,
    eyeClosed,
};